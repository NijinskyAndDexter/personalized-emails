const BackgroundRemover = () => {
  return (
    <div style={{height: '100vh', width: '100vw'}}>
      <div>Background Remover</div>
      <p>TBH lots to do here but not doing it now</p>
    </div>
  );
}

export default BackgroundRemover;